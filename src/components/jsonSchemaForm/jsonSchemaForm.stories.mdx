import { Meta, Preview, Props, Description, Story } from "@storybook/addon-docs/blocks";
import { object } from "@storybook/addon-knobs";
import { JsonSchemaForm } from "./";
import { data } from "./sample";

<Meta title="MDX|JsonSchemaForm" component={JsonSchemaForm} />

# JsonSchemaForm component

<Description of={JsonSchemaForm} />

# Props

<Props of={JsonSchemaForm} />

# Usage

With `MDX` we can define a story for `JsonSchemaForm` right in the middle of our
markdown documentation.

<Preview>
  <Story name="with initial value">
    <JsonSchemaForm formSchema={object("formSchema", data)} onSubmit={() => console.log("submit")} />
  </Story>
</Preview>
